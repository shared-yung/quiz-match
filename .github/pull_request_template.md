<!--
タイトルは Conventional Commits 形式で書いてください（squash merge でコミット件名になります）。
  例: feat(quiz): 採点ロジックを追加
-->

## 対応する issue

Closes #

<!-- 上の行は必須です。issue がリンクされていない PR は CI で落ちます。 -->

## 変更内容

-

## 計画との差異

<!--
issue 発行時の計画と実際の実装がずれた点と、その経緯を書く。
実装中に計画が変わること自体は問題ない。記録が残らないことが問題。

差異がなければ「なし」とだけ書く。空欄のままだと CI で落ちる。
マージ時にこの節がリンク先の issue へ自動でコメントされる。
-->

## 確認したこと

- [ ] `bun run lint` が通る
- [ ] `bun run typecheck` が通る
- [ ] `bun run test` が通る
- [ ] オニオンの依存方向を壊していない（domain が外側を参照していない）
