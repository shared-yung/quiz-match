{
  "permissions": {
    "deny": ["Bash(git push --force*)", "Bash(git push -f *)", "Bash(gh repo delete*)"],
    "ask": ["Bash(git push)", "Bash(git push *)"]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/guard-remote.mjs",
            "timeout": 10,
            "statusMessage": "リモート操作を確認中"
          }
        ]
      }
    ]
  }
}
